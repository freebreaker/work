export const login = require('./login.ts')

export const logout = require('./logout.ts')

export const petType = require('./petType.ts')

export const good = require('./good.ts')

export const service = require('./service.ts')

export const appAndApk = require('./app.ts')

export const audit = require('./audit.ts')

export const detail = require('./detail.ts')

export const role = require('./role.ts')

export const menu = require('./menu.ts')

export const user = require('./user.ts')

export const chart = require('./chart.ts')

export const vaccine = require('./vaccine.ts')

export const repellant = require('./repellant.ts')

export const adminusers = require('./adminusers.ts')

export const round = require('./round.ts')

export const picture = require('./picture.ts')

export const personalRound = require('./personalRound.ts')

export const advertisement = require('./advertisement.ts')

export const inform = require('./inform.ts')

export const recommend = require('./recommend.ts')